
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>For Nandinii 💕</title>

    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI',sans-serif;
            background: linear-gradient(135deg,#ffe6f0,#ffd6eb,#ffc2e2);
            overflow: hidden;
            color: #ff4d6d;
            text-align: center;
        }

        /* soft floating animation */
        @@keyframes float {
            0%

        {
            transform: translateY(0px);
        }

        50% {
            transform: translateY(-10px);
        }

        100% {
            transform: translateY(0px);
        }

        }

        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        h1 {
            font-size: 48px;
            margin-bottom: 10px;
            animation: float 3s ease-in-out infinite;
        }

        .typing {
            font-size: 20px;
            max-width: 600px;
            min-height: 100px;
            margin: 20px auto;
            color: #d63384;
        }

        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 30px;
            background: #ff4d6d;
            color: white;
            font-size: 18px;
            cursor: pointer;
            transition: .3s;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

            .btn:hover {
                transform: scale(1.1);
            }

        /* cute polaroid style slider */
        .slider {
            margin-top: 40px;
            display: flex;
            gap: 20px;
            animation: float 5s ease-in-out infinite;
        }

            .slider img {
                width: 150px;
                border-radius: 15px;
                box-shadow: 0 10px 25px rgba(0,0,0,0.2);
                transform: rotate(-3deg);
            }

                .slider img:nth-child(2) {
                    transform: rotate(3deg);
                }

                .slider img:nth-child(3) {
                    transform: rotate(-2deg);
                }

                .slider img:nth-child(4) {
                    transform: rotate(2deg);
                }

        /* cute floating hearts */
        .heart {
            position: fixed;
            bottom: -20px;
            font-size: 20px;
            animation: rise 6s linear infinite;
            opacity: 0.6;
        }

        @@keyframes rise {
            from

        {
            transform: translateY(0);
        }

        to {
            transform: translateY(-120vh);
        }

        }

        /* sparkles */
        .sparkle {
            position: fixed;
            width: 5px;
            height: 5px;
            background: white;
            border-radius: 50%;
            opacity: 0.7;
        }
    </style>
</head>

<body>

    <audio id="music" loop>
        <source src="/music/love.mp3" type="audio/mpeg">
    </audio>

    <div class="hero">

        <h1>Nandinii 💕</h1>

        <div class="typing" id="typingText"></div>

        <button class="btn" onclick="enter()">Enter Our Story 💘</button>

      @*   <div class="slider">
            <img src="/images/1.jpg" />
            <img src="/images/2.jpg" />
            <img src="/images/3.jpg" />
            <img src="/images/4.jpg" />
        </div> *@

    </div>

    <script>

        const text = `
        Hi Beautiful 😌

        I didn’t just make a website.

        I made a memory.

        From the moment you walked into my life,
        something shifted.

        You became my favorite notification.
        My calm in chaos.
        My smile without trying.

        And honestly?
        I wouldn’t change a single thing.
        `;

        let i = 0;
        function typeWriter(){
            if(i < text.length){
                document.getElementById("typingText").innerHTML += text.charAt(i);
                i++;
                setTimeout(typeWriter,30);
            }
        }
        typeWriter();

        function enter(){
            document.getElementById("music").play();
            window.location="/Home/OurStory";
        }

        /* floating hearts */
        setInterval(()=>{
            const heart=document.createElement("div");
            heart.className="heart";
            heart.innerHTML="💖";
            heart.style.left=Math.random()*100+"vw";
            document.body.appendChild(heart);
            setTimeout(()=>heart.remove(),6000);
        },500);

        /* sparkles */
        setInterval(()=>{
            const sparkle=document.createElement("div");
            sparkle.className="sparkle";
            sparkle.style.left=Math.random()*100+"vw";
            sparkle.style.top=Math.random()*100+"vh";
            document.body.appendChild(sparkle);
            setTimeout(()=>sparkle.remove(),2000);
        },300);

    </script>

</body>
</html>
