
<!DOCTYPE html>
<html>
<head>
    <title>Final Lock 🔐</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            height: 100vh;
            font-family: 'Segoe UI', sans-serif;
            background: radial-gradient(circle at top, #2b1055, #000000);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        /* Soft floating hearts */
        .heart {
            position: absolute;
            bottom: -40px;
            font-size: 20px;
            animation: floatUp 12s linear infinite;
            opacity: 0.5;
        }

        @@keyframes floatUp {
            from

        {
            transform: translateY(0) scale(1);
            opacity: 0;
        }

        to {
            transform: translateY(-120vh) scale(1.6);
            opacity: 1;
        }

        }

        .card {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(15px);
            border-radius: 30px;
            padding: 60px 50px;
            text-align: center;
            width: 380px;
            box-shadow: 0 25px 60px rgba(0,0,0,0.6);
            animation: fadeIn 1.2s ease;
        }

        @@keyframes fadeIn {
            from

        {
            opacity: 0;
            transform: translateY(40px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        h1 {
            font-size: 38px;
            margin-bottom: 10px;
        }

            h1 span {
                color: #ff6b81;
            }

        p {
            font-size: 16px;
            opacity: 0.9;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        input {
            width: 100%;
            padding: 14px 18px;
            border-radius: 30px;
            border: none;
            outline: none;
            font-size: 16px;
            text-align: center;
            margin-bottom: 20px;
        }

        button {
            padding: 14px 35px;
            border-radius: 30px;
            border: none;
            font-size: 16px;
            background: linear-gradient(135deg, #ff4d6d, #ff758f);
            color: white;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
            box-shadow: 0 12px 30px rgba(255,77,109,0.6);
        }

            button:hover {
                transform: scale(1.1);
                box-shadow: 0 18px 40px rgba(255,77,109,0.9);
            }

        footer {
            margin-top: 25px;
            font-size: 13px;
            opacity: 0.6;
        }
    </style>
</head>

<body>

    <!-- Floating hearts -->
    <span class="heart" style="left:10%;">❤️</span>
    <span class="heart" style="left:30%;">💖</span>
    <span class="heart" style="left:50%;">💘</span>
    <span class="heart" style="left:70%;">❤️</span>
    <span class="heart" style="left:90%;">✨</span>

    <div class="card">
        <h1>Final Lock <span>🔐</span></h1>

        <p>
            This lock doesn’t need a password.<br>
            It needs **a name said with love** 😌
        </p>

        <input id="code" placeholder="What does I call you?" />

        <button onclick="unlock()">Unlock My Heart ❤️</button>

        <footer>
            One last step… then forever 💍
        </footer>
    </div>

    <script>
        function unlock() {
            const value = document.getElementById("code").value.toLowerCase().trim();

            if (value === "battli") {
                window.location.href = "/Home/Forever";
            } else {
                alert("Wrong 😏\nTry again… my heart knows the answer.");
            }
        }
    </script>

</body>
</html>
